<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="roboguice.calculator" android:versionCode="1" android:versionName="1.0-SNAPSHOT">

  <application android:icon="@drawable/icon" android:label="@string/app_name">
    <activity android:name=".activity.CalculatorActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

	<uses-library android:name="roboguice.2_0"/>
	<uses-library android:name="guice.3.no.aop"/>
	<uses-library android:name="javax.inject"/>
  <uses-library android:name="android.support.v4"/>

  </application>

</manifest>

